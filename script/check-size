#!/bin/bash
mkdir -p /tmp/ot-size-report
cat > /tmp/ot-size-report/report_pr << 'PAYLOAD'
<!-- Size Report of **OpenThread** -->
⚠️ POC-BY-muhammadimamrisky20-code: injected via script/check-size artifact poisoning. Canary only, no real payload. See linked security report for details.
PAYLOAD
echo "${PR_NUMBER}" > /tmp/ot-size-report/pr_number
